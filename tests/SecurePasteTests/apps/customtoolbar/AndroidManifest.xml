<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="grapheneos.securepaste.customtoolbar">
    <application
        android:debuggable="true"
        android:label="Secure Paste Custom Toolbar"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name="grapheneos.securepaste.helper.SecurePasteActivity"
            android:exported="true" />
        <provider
            android:name="grapheneos.securepaste.helper.SecurePasteCommandProvider"
            android:authorities="grapheneos.securepaste.customtoolbar.provider"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>
</manifest>
