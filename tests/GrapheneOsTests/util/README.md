# grapheneos-test-common

Test utilities for GrapheneOS-specific tests

Based on

- CTS tests (`cts/` and `packages/modules/Permission/tests/cts`)
- permissionTestUtilLib (`packages/modules/Permission/tests/cts/permission/permissionTestUtilLib`)